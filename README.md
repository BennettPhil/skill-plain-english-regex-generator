# Plain English Regex Generator

Convert short plain-English matching requests into practical regex patterns.

Quick start:

```bash
chmod +x scripts/run.py
./scripts/run.py "match an email address"
./scripts/run.py --format json "find all dates in MM/DD/YYYY format"
```

Prerequisites:

- `python3`
